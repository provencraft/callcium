pre-commit:
  commands:
    forge-fmt:
      glob: "src/**/*.sol test/**/*.sol"
      run: forge fmt --check {staged_files}

    docs-fmt:
      root: "docs/"
      glob: "docs/**/*.{ts,tsx,mjs,css,json,md,mdx}"
      run: bunx oxfmt --check {staged_files}

    docs-lint:
      root: "docs/"
      glob: "docs/**/*.{ts,tsx}"
      run: bunx oxlint {staged_files}

    sdk-fmt:
      root: "sdk/"
      glob: "sdk/**/*.{ts,json,md}"
      run: bunx oxfmt --check {staged_files}

    sdk-lint:
      root: "sdk/"
      glob: "sdk/**/*.ts"
      run: bunx oxlint {staged_files}
