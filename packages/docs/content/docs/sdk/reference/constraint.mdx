---
title: "Constraint"
description: "Constraint helpers and builder types."
---

## Functions

### arg

Target a calldata argument by path.
Each argument is a big-endian uint16 step; multiple steps navigate into nested types.

[Git Source](https://github.com/provencraft/callcium/blob/main/packages/sdk/src/constraint.ts#L327)

```ts
function arg(p0: number, ...rest: number[]): ConstraintBuilder;
```

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `p0` | `number` | - |
| `rest` | `number[]` | - |

**Returns**

`ConstraintBuilder`

### msgSender

Target the `msg.sender` context property.

[Git Source](https://github.com/provencraft/callcium/blob/main/packages/sdk/src/constraint.ts#L333)

```ts
function msgSender(): ConstraintBuilder<`0x${string}`>;
```

**Returns**

`` ConstraintBuilder<`0x${string}`> ``

### msgValue

Target the `msg.value` context property.

[Git Source](https://github.com/provencraft/callcium/blob/main/packages/sdk/src/constraint.ts#L338)

```ts
function msgValue(): ConstraintBuilder<bigint>;
```

**Returns**

`ConstraintBuilder<bigint>`

### blockTimestamp

Target the `block.timestamp` context property.

[Git Source](https://github.com/provencraft/callcium/blob/main/packages/sdk/src/constraint.ts#L343)

```ts
function blockTimestamp(): ConstraintBuilder<bigint>;
```

**Returns**

`ConstraintBuilder<bigint>`

### blockNumber

Target the `block.number` context property.

[Git Source](https://github.com/provencraft/callcium/blob/main/packages/sdk/src/constraint.ts#L348)

```ts
function blockNumber(): ConstraintBuilder<bigint>;
```

**Returns**

`ConstraintBuilder<bigint>`

### chainId

Target the `block.chainid` context property.

[Git Source](https://github.com/provencraft/callcium/blob/main/packages/sdk/src/constraint.ts#L353)

```ts
function chainId(): ConstraintBuilder<bigint>;
```

**Returns**

`ConstraintBuilder<bigint>`

### txOrigin

Target the `tx.origin` context property.

[Git Source](https://github.com/provencraft/callcium/blob/main/packages/sdk/src/constraint.ts#L358)

```ts
function txOrigin(): ConstraintBuilder<`0x${string}`>;
```

**Returns**

`` ConstraintBuilder<`0x${string}`> ``

### baseFee

Target the `block.basefee` context property.

[Git Source](https://github.com/provencraft/callcium/blob/main/packages/sdk/src/constraint.ts#L363)

```ts
function baseFee(): ConstraintBuilder<bigint>;
```

**Returns**

`ConstraintBuilder<bigint>`

### gasPrice

Target the `tx.gasprice` context property.

[Git Source](https://github.com/provencraft/callcium/blob/main/packages/sdk/src/constraint.ts#L368)

```ts
function gasPrice(): ConstraintBuilder<bigint>;
```

**Returns**

`ConstraintBuilder<bigint>`

## Types

### ConstraintBuilder

Mutable builder that accumulates operators targeting a single path.
Implements the `Constraint` interface so it can be passed directly to policy builders.

[Git Source](https://github.com/provencraft/callcium/blob/main/packages/sdk/src/constraint.ts#L155)

### ScalarValue

Accepted scalar value types for operator arguments.

[Git Source](https://github.com/provencraft/callcium/blob/main/packages/sdk/src/constraint.ts#L13)
