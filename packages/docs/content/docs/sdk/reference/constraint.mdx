---
title: "Constraint"
description: "Constraint helpers and builder types."
---

## Functions

### arg

Target a calldata argument by path.
Each argument is a big-endian uint16 step; multiple steps navigate into nested types.

[Git Source](https://github.com/provencraft/callcium/blob/main/packages/sdk/src/constraint.ts#L270)

```ts
function arg(p0: number): ConstraintBuilder;
```

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `p0` | `number` | - |

**Returns**

`ConstraintBuilder`

### msgSender

Target the `msg.sender` context property.

[Git Source](https://github.com/provencraft/callcium/blob/main/packages/sdk/src/constraint.ts#L279)

```ts
function msgSender(): ConstraintBuilder;
```

**Returns**

`ConstraintBuilder`

### msgValue

Target the `msg.value` context property.

[Git Source](https://github.com/provencraft/callcium/blob/main/packages/sdk/src/constraint.ts#L284)

```ts
function msgValue(): ConstraintBuilder;
```

**Returns**

`ConstraintBuilder`

### blockTimestamp

Target the `block.timestamp` context property.

[Git Source](https://github.com/provencraft/callcium/blob/main/packages/sdk/src/constraint.ts#L289)

```ts
function blockTimestamp(): ConstraintBuilder;
```

**Returns**

`ConstraintBuilder`

### blockNumber

Target the `block.number` context property.

[Git Source](https://github.com/provencraft/callcium/blob/main/packages/sdk/src/constraint.ts#L294)

```ts
function blockNumber(): ConstraintBuilder;
```

**Returns**

`ConstraintBuilder`

### chainId

Target the `block.chainid` context property.

[Git Source](https://github.com/provencraft/callcium/blob/main/packages/sdk/src/constraint.ts#L299)

```ts
function chainId(): ConstraintBuilder;
```

**Returns**

`ConstraintBuilder`

### txOrigin

Target the `tx.origin` context property.

[Git Source](https://github.com/provencraft/callcium/blob/main/packages/sdk/src/constraint.ts#L304)

```ts
function txOrigin(): ConstraintBuilder;
```

**Returns**

`ConstraintBuilder`

## Types

### ConstraintBuilder

Mutable builder that accumulates operators targeting a single path.
Implements the `Constraint` interface so it can be passed directly to policy builders.

[Git Source](https://github.com/provencraft/callcium/blob/main/packages/sdk/src/constraint.ts#L124)

### ScalarValue

Accepted scalar value types for operator arguments.

[Git Source](https://github.com/provencraft/callcium/blob/main/packages/sdk/src/constraint.ts#L12)
