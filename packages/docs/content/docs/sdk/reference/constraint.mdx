---
title: "Constraint"
description: "Constraint helpers and builder types."
---

## Functions

### arg

Target a calldata argument by path.
Each argument is a big-endian uint16 step; multiple steps navigate into nested types.

[Git Source](https://github.com/provencraft/callcium/blob/main/packages/sdk/src/constraint.ts#L272)

```ts
function arg(p0: number, ...rest: number[]): ConstraintBuilder;
```

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `p0` | `number` | - |
| `rest` | `number[]` | - |

**Returns**

`ConstraintBuilder`

### msgSender

Target the `msg.sender` context property.

[Git Source](https://github.com/provencraft/callcium/blob/main/packages/sdk/src/constraint.ts#L278)

```ts
function msgSender(): ConstraintBuilder;
```

**Returns**

`ConstraintBuilder`

### msgValue

Target the `msg.value` context property.

[Git Source](https://github.com/provencraft/callcium/blob/main/packages/sdk/src/constraint.ts#L283)

```ts
function msgValue(): ConstraintBuilder;
```

**Returns**

`ConstraintBuilder`

### blockTimestamp

Target the `block.timestamp` context property.

[Git Source](https://github.com/provencraft/callcium/blob/main/packages/sdk/src/constraint.ts#L288)

```ts
function blockTimestamp(): ConstraintBuilder;
```

**Returns**

`ConstraintBuilder`

### blockNumber

Target the `block.number` context property.

[Git Source](https://github.com/provencraft/callcium/blob/main/packages/sdk/src/constraint.ts#L293)

```ts
function blockNumber(): ConstraintBuilder;
```

**Returns**

`ConstraintBuilder`

### chainId

Target the `block.chainid` context property.

[Git Source](https://github.com/provencraft/callcium/blob/main/packages/sdk/src/constraint.ts#L298)

```ts
function chainId(): ConstraintBuilder;
```

**Returns**

`ConstraintBuilder`

### txOrigin

Target the `tx.origin` context property.

[Git Source](https://github.com/provencraft/callcium/blob/main/packages/sdk/src/constraint.ts#L303)

```ts
function txOrigin(): ConstraintBuilder;
```

**Returns**

`ConstraintBuilder`

### baseFee

Target the `block.basefee` context property.

[Git Source](https://github.com/provencraft/callcium/blob/main/packages/sdk/src/constraint.ts#L308)

```ts
function baseFee(): ConstraintBuilder;
```

**Returns**

`ConstraintBuilder`

### gasPrice

Target the `tx.gasprice` context property.

[Git Source](https://github.com/provencraft/callcium/blob/main/packages/sdk/src/constraint.ts#L313)

```ts
function gasPrice(): ConstraintBuilder;
```

**Returns**

`ConstraintBuilder`

## Types

### ConstraintBuilder

Mutable builder that accumulates operators targeting a single path.
Implements the `Constraint` interface so it can be passed directly to policy builders.

[Git Source](https://github.com/provencraft/callcium/blob/main/packages/sdk/src/constraint.ts#L126)

### ScalarValue

Accepted scalar value types for operator arguments.

[Git Source](https://github.com/provencraft/callcium/blob/main/packages/sdk/src/constraint.ts#L12)
